#include<iostream>
#include<vector>
using namespace std;
//data structure: binary search tree(BST)

int main(){


    //constraints to be obeyed
    //reserve request for landings : landing time t
    //add t to set r of landinf times if no other landings are scheduled within k minutes
    //remove from set r after plane lands

    //bST allow fast insertion! 
    //invariant associated with it
   vector<float> r;
   float time;
   cin>>time;
   for(int i = 0; i<r.size(); i++)





    return 0;
}